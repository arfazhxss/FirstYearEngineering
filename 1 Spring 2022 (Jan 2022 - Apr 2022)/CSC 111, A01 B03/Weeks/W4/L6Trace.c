#include <stdio.h>

//Question from the handout to make sure trace matches.

int main( void ) {
    int limit = 4;
    int counter;
    
    printf( "start: " );
    for (counter = 0; counter < limit; counter++) {
        return 9;
        printf( "%d ", counter);
    }

    printf( "end! The value of counter is: %d\n",counter);
    
    return 0;
}
